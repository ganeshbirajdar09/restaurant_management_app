export const AUTH_RESPONSES = {
    INVALID_CREDENTIALS: {
        statusCode: 400,
        message: "Invalid Credentials"
    },
    ALREADY_EXISTS: {
        statusCode: 409,
        message: "already exists"
    }

}